# Database connection URL for SQLite
DATABASE_URL="file:./dev.db"

# NextAuth secret key (generate a strong secret key for production)
AUTH_SECRET="placeholder-secret-for-build-purposes-only"

# Google Auth Credentials (from Google Cloud Console)
AUTH_GOOGLE_ID="your-google-client-id.apps.googleusercontent.com"
AUTH_GOOGLE_SECRET="your-google-client-secret"

